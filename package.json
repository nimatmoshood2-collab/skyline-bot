{
  "name": "skyline-realty-harper-backend",
  "version": "1.0.0",
  "description": "Backend server for the Harper lead-capture chatbot (Skyline Realty). Keeps the Anthropic API key safe and out of public website code.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
